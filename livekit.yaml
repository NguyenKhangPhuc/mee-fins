port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false  # false vì đang chạy local dev
room:
  empty_timeout: 300      # giây — room tự đóng sau 5 phút nếu trống (không ai join)
  departure_timeout: 20
keys:
  devkey: secretsecretsecretsecretsecret32

webhook:
  api_key: devkey
  urls:
    - http://host.docker.internal:3001/webhooks/livekit