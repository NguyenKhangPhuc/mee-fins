# Caddy Configuration for meefins.com Production Setup

# NestJS Backend API
api.meefins.online {
    encode gzip zstd
    reverse_proxy backend:3001
}

# LiveKit WebRTC Signaling & HTTP Server
livekit.meefins.online {
    encode gzip zstd
    reverse_proxy livekit:7880
}
